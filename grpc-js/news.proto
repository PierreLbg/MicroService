syntax = "proto3";

service Traitement {
    rpc Action (InputMessage) returns (OutputMessage) {}
}

message InputMessage {
    string message = 1;
}

message OutputMessage {
   string message = 1;
   string AR = 2;
}